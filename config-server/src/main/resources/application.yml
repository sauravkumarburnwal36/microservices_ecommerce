spring.application.name: config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URL}
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
          default-label: main

server:
  port: 8888
